spring:
  cloud:
    config:
      profile: dev
      discovery:
        enabled: true
        uri: http://localhost:10010/
      username: ${CONFIG_USERNAME:user}
      password: ${CONFIG_PASSWORD:admin}
  application:
    name: manage-registry

security:
  basic:
    enabled: true
  user:
    name: ${REGISTRY_USERNAME:user}
    password: ${REGISTRY_PASSWORD:admin}
server:
  port: 10011


